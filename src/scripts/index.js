// DEPENDENCIES IMPORTS (jquery and lodash are loaded globally with the webpack provide plugin)
import 'jquery.scrollto';
import 'bootstrap';

// STYLES DEPENDENCIES IMPORTS
import '../styles/main.scss';

console.log(`JQuery version : ${jQuery.fn.jquery}`);
console.log(`lodash version : ${_.VERSION}`);
